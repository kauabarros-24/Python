#A última forma básica de armazenar coleções de dados em Python é utilizando tuplas. Elas são parecidas com listas, porém, são imutáveis! Ou seja, uma vez criadas, não podem ser modificadas.
# Além disso, é uma estrutura ordenada, isso é, os itens permanecem na ordem definida, e é permitido elementos duplicados, uma vez que uma tupla é indexada

tupla = (1, 2)
print(type(tupla)) # Imprime tuple
tuple = (1)
